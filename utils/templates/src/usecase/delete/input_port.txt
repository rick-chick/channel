from abc import ABC, abstractmethod
from channel.usecase.models import ({model}DeleteInDto)

from typing import List


class {model}DeleteInputPort(ABC):

    @abstractmethod
    def delete(self, {model_lower}_dto: {model}DeleteInDto) -> List[int]:
        pass
