from abc import ABC, abstractmethod

from typing import List


class {model}DeleteOututPort(ABC):

    @abstractmethod
    def prepare_success_view(self, {model_lower}_ids: List[int]) -> List[int]:
        pass

    @abstractmethod
    def prepare_fail_view(self, exception: Exception):
        pass
